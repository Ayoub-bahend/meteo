{{- if .Values.weatherService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-service
  namespace: {{ .Values.namespace | default "default" }}
  labels:
    {{- include "meteo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: weather-service
spec:
  replicas: {{ .Values.weatherService.replicaCount }}
  selector:
    matchLabels:
      {{- include "meteo-app.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: weather-service
  template:
    metadata:
      labels:
        {{- include "meteo-app.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: weather-service
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: weather-service
          image: "{{ .Values.global.awsAccountId }}.dkr.ecr.{{ .Values.global.awsRegion }}.amazonaws.com/{{ .Values.global.ecrRepositoryPrefix }}-weather-service:{{ .Values.weatherService.image.tag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.weatherService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /api/weather/Paris
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/weather/Paris
              port: http
            initialDelaySeconds: 20
            periodSeconds: 5
          resources:
            {{- toYaml .Values.weatherService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: weather-service
  namespace: {{ .Values.namespace | default "default" }}
  labels:
    {{- include "meteo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: weather-service
spec:
  type: {{ .Values.weatherService.service.type }}
  ports:
    - port: {{ .Values.weatherService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meteo-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: weather-service
{{- end }}

